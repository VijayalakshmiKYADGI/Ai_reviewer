version: '3.8'
services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - .:/app
  redis:
    image: redis:7-alpine
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: reviews
      POSTGRES_USER: crewai
      POSTGRES_PASSWORD: crewai
